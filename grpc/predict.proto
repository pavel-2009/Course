syntax = "proto3";

package predictor;

service Predictor
{
    rpc Predict(PredictRequest)
        returns (PredictResponse);
}

message PredictRequest
{
    string text = 1;
}

message PredictResponse
{
    string label = 1;
    float score = 2;
}